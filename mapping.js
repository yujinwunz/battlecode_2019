// Mapping part of robotics.
// We know coordinates and map layout. What we don't know is the units.



// Function that takes all visual input and then makes inferences on that.
// This is general for all bots and only takes into account vision,
// not communication.
export function observe(game) {

}

// If we know the existence of a unit by other means (like communication 
// or logical inference) add it here. Stable things only.
export function add_unit(robot) {

}

// If we know the death of a unit by other means (like communication or
// logical inference) add it here. 
export function remove_unit(loc) {

}

export function get_known_structures(team) {

}
